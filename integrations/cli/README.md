# Ship CLI

These are the canon-required commands. This list does not rule out future CLI
commands once real flows need them.

Required commands:

- `ship login`
- `ship docs ls`
- `ship docs get`
- `ship docs create`
- `ship webhooks tail` (`--once` auto-cleans its subscription unless `--keep-subscription` is set)
