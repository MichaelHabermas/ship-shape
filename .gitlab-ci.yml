image: node:20-bookworm

stages:
  - submission

variables:
  CI: "true"
  POSTGRES_DB: ship_test_audit
  POSTGRES_USER: ship
  POSTGRES_PASSWORD: ship_dev_password
  DATABASE_URL: postgresql://ship:ship_dev_password@postgres:5432/ship_test_audit
  TTFE_DATABASE_URL: postgresql://ship:ship_dev_password@postgres:5432/ship_test_audit

.default_submission_job:
  stage: submission
  image: node:20-bookworm
  services:
    - name: postgres:16
      alias: postgres
    - name: docker:24-dind
      alias: docker
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
  cache:
    key:
      files:
        - pnpm-lock.yaml
    paths:
      - .pnpm-store/
      - .pnpm/
  before_script:
    - export PNPM_HOME="$CI_PROJECT_DIR/.pnpm"
    - export PATH="$PNPM_HOME:$PATH"
    - corepack enable
    - corepack prepare pnpm@10.27.0 --activate
    - pnpm config set store-dir .pnpm-store
    - apt-get update
    - apt-get install -y --no-install-recommends postgresql-client docker.io
    - rm -rf /var/lib/apt/lists/*
    - |
      for attempt in $(seq 1 30); do
        if pg_isready -h postgres -U "$POSTGRES_USER" -d "$POSTGRES_DB"; then
          break
        fi
        if [ "$attempt" = "30" ]; then
          echo "Postgres service did not become ready after 30 attempts"
          exit 1
        fi
        sleep 1
      done
    - |
      for attempt in $(seq 1 30); do
        if docker info >/dev/null 2>&1; then
          break
        fi
        if [ "$attempt" = "30" ]; then
          echo "Docker service did not become ready after 30 attempts"
          exit 1
        fi
        sleep 1
      done
    - pnpm install --frozen-lockfile

plugforge-submission:
  extends: .default_submission_job
  script:
    - node ./scripts/ci/plugforge-submission.mjs
